{
  "_comment": "Genereres ved container-start fra env-vars via /docker-entrypoint.d/40-runtime-config.sh. Felter merges ind i Angular's environment-objekt før bootstrap.",
  "nexusToken": "${NEXUS_TOKEN}",
  "registryUrl": "${REGISTRY_URL}"
}
