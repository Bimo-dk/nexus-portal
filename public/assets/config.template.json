{
  "_comment": "Generated at container start from env-vars via /docker-entrypoint.d/40-runtime-config.sh. Fields are merged into Angular's environment object before bootstrap.",
  "nexusToken": "${NEXUS_TOKEN}",
  "registryUrl": "${REGISTRY_URL}"
}
